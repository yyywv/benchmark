# Group B —— transformers 5.15.0
#
# 给 4.57.6 加载不了的新架构用。已实测：Cosmos3-Edge 在 5.15.0 上构造成功
# （2.44B 参数），在 4.57.6 上报「架构 cosmos3_edge 未注册」。
#
# 注意 huggingface_hub 是 1.x —— transformers 5.x 要求它，而 Group A 的
# 0.36.2 与之不兼容。这也是两套环境必须物理隔离、不能靠一套环境切换的原因之一。
#
# python 3.11，与 Group A 一致，便于共享 uv 的下载缓存。

torch==2.6.0
torchvision==0.21.0
transformers==5.15.0
tokenizers==0.22.2
accelerate==1.6.0
safetensors==0.8.0
huggingface_hub==1.27.0

qwen-vl-utils==0.0.14
decord==0.6.0
av==18.1.0
opencv-python-headless==4.11.0.86
pillow==11.1.0
timm==1.0.15
einops==0.8.1
sentencepiece==0.2.0

numpy==1.26.4

requests==2.32.3
