# Local agent/cache files
.agents/
.cache/
.codex

# Python caches
__pycache__/
*.py[cod]

# Large local data and generated outputs
datasets/
workflow_outputs/
results/
models/

# Backups left by the QA path-normalization pass (BC-08)
*.orig

# uv 建的虚拟环境（每套几 GB，由 envs/*.txt 重建）
.venvs/

# Common local/runtime artifacts
.env
.venv/
venv/
*.log
